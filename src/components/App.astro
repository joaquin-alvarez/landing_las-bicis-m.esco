---
---

<section class="app-section" id="app">
	<div class="container">
		<div class="app-layout">
			<div class="app-phone">
				<div class="phone-mockup">
					<div class="phone-screen">
						[APP SCREENSHOT]
					</div>
				</div>
			</div>
			<div class="app-content">
				<h2>Tu compañera de viaje</h2>
				<p class="app-description">
					Con nuestra app puedes encontrar bicicletas disponibles cerca de ti, 
					planificar tu ruta y gestionar todos tus viajes de forma sencilla.
				</p>
				<ul class="app-features">
					<li>📍 Localiza estaciones en tiempo real</li>
					<li>🚲 Verifica disponibilidad de bicicletas</li>
					<li>🗺️ Planifica tu ruta óptima</li>
					<li>📊 Consulta tu historial de viajes</li>
					<li>💳 Gestiona tus pagos y suscripciones</li>
				</ul>
				<div class="download-buttons">
					<a href="#" class="download-btn">
						<span class="btn-text">Descargar en App Store</span>
					</a>
					<a href="#" class="download-btn">
						<span class="btn-text">Descargar en Google Play</span>
					</a>
				</div>
			</div>
		</div>
	</div>
</section>

<style>
	.app-section {
		background-color: var(--white);
	}

	.app-layout {
		display: grid;
		grid-template-columns: 1fr;
		gap: 3rem;
		align-items: center;
	}

	.phone-mockup {
		width: 250px;
		height: 500px;
		background: linear-gradient(45deg, var(--gray-800), var(--gray-600));
		border-radius: 2rem;
		padding: 1rem;
		margin: 0 auto;
		box-shadow: var(--shadow-lg);
		position: relative;
	}

	.phone-screen {
		width: 100%;
		height: 100%;
		background-color: var(--light-blue);
		border-radius: 1.5rem;
		display: flex;
		align-items: center;
		justify-content: center;
		font-weight: 500;
		color: var(--primary-blue);
	}

	.app-content {
		text-align: center;
	}

	.app-content h2 {
		color: var(--primary-blue);
		margin-bottom: 1.5rem;
	}

	.app-description {
		font-size: 1.1rem;
		color: var(--gray-600);
		line-height: 1.6;
		margin-bottom: 2rem;
	}

	.app-features {
		list-style: none;
		margin-bottom: 3rem;
	}

	.app-features li {
		padding: 0.5rem 0;
		font-size: 1rem;
		color: var(--gray-700);
	}

	.download-buttons {
		display: flex;
		flex-direction: column;
		gap: 1rem;
		align-items: center;
	}

	.download-btn {
		display: inline-block;
		padding: 0.75rem 1.5rem;
		background-color: var(--gray-800);
		color: var(--white);
		border-radius: var(--border-radius);
		transition: all 0.3s ease;
		min-width: 200px;
	}

	.download-btn:hover {
		background-color: var(--primary-blue);
		transform: translateY(-2px);
		box-shadow: var(--shadow-md);
		color: var(--white);
	}

	@media (min-width: 768px) {
		.app-layout {
			grid-template-columns: 1fr 1.5fr;
		}

		.app-content {
			text-align: left;
		}

		.download-buttons {
			flex-direction: row;
			justify-content: flex-start;
		}
	}

	@media (min-width: 1024px) {
		.phone-mockup {
			width: 300px;
			height: 600px;
		}
	}
</style>
