---
---

<section class="pricing-section" id="precios">
	<div class="container">
		<div class="section-header">
			<h2>¿Cuánto cuesta?</h2>
			<p>Tenemos varios planes para que elijas el que mas te convenga. Registrate y seleccioná tu plan en la app. <br>Te recordamos que <b>podés hacer un máximo de 4 viajes de 60 minutos cada uno por día</b>, de superar este límite <u>serás multado</u>.</p>
		</div>
		
		<div class="pricing-circles">
			<!-- Plan por hora (blanco) -->
			<div class="pricing-circle blue">
				<div class="price-content">
					<div class="price">$225/hora</div>
					<div class="description">Te lo recomendamos para un viaje ocasional.</div>
				</div>
			</div>

			<!-- Plan por día -->
			<div class="pricing-circle blue">
				<div class="price-content">
					<div class="price">$450/día</div>
					<div class="description">Ideal para moverte durante todo el día.
						<span class="small">*</span>
					</div>
				</div>
				<div class="pricing-popup">
					<p>Podés hacer hasta 4 viajes de 60 minutos por día</p>
				</div>
			</div>

			<!-- Plan mensual -->
			<div class="pricing-circle blue">
				<div class="price-content">
					<div class="price">$6800/mes</div>
					<div class="description">Animate y subite a la movilidad sustentable.
					<span class="small">*</span>
					</div>
				</div>
				<div class="pricing-popup">
					<p>Podés hacer hasta 4 viajes de 60 minutos por día</p>
				</div>
			</div>

			<!-- Plan semestral -->
			<div class="pricing-circle blue">
				<div class="price-content">
					<div class="price">$36100/<br>semestre</div>
					<div class="description">Bajate del auto y viajá saludable.
					<span class="small">*</span>
					</div>
				</div>
				<div class="pricing-popup">
					<p>Podés hacer hasta 4 viajes de 60 minutos por día</p>
				</div>
			</div>

			<!-- Plan anual -->
			<div class="pricing-circle blue">
				<div class="price-content">
					<div class="price">$67600/<br>año</div>
					<div class="description">Transformá la movilidad de Escobar.
					<span class="small">*</span>
				</div>
				</div>
				<div class="pricing-popup">
					<p>Podés hacer hasta 4 viajes de 60 minutos por día</p>
				</div>
			</div>
		</div>
	</div>
</section>

<style>
	.pricing-section {
		background-color: var(--white);
		padding: 4rem 0;
	}

	.section-header {
		text-align: center;
		margin-bottom: 4rem;
	}

	.section-header h2 {
		color: var(--primary-blue);
		margin-bottom: 1rem;
	}

	.section-header p {
		font-size: 1.1rem;
		color: var(--primary-blue);
		max-width: 600px;
		margin: 0 auto;
	}

	.pricing-circles {
		display: flex;
		flex-wrap: wrap;
		justify-content: center;
		gap: 5vw;
		max-width: 1200px;
		margin: 0 auto;
		padding: 0 1rem;
	}

	.pricing-circle {
		width: 260px;
		height: 260px;
		border-radius: 50%;
		display: flex;
		align-items: center;
		justify-content: center;
		text-align: center;
		position: relative;
		padding: 1.5rem;
		box-shadow: 0 4px 10px rgba(0, 0, 0, 0.1);
		transition: all 0.4s ease-in-out;
	}

	.pricing-circle.blue:hover {
		/* Apply white style on hover */
		border: 4px solid transparent;
		background-image: linear-gradient(white, white), linear-gradient(250deg, var(--primary-blue), var(--primary-blue), transparent);
		background-origin: border-box;
		background-clip: padding-box, border-box;
		background-color: white;
		animation: pricing-pulse 0.5s ease-out;
	}
	
	@keyframes pricing-pulse {
		0% { transform: scale(1); }
		50% { transform: scale(1.05); }
		100% { transform: scale(1); }
	}

	/* Pricing circle hover state will change text colors */
	.pricing-circle.blue:hover .price {
		color: var(--primary-blue);
	}

	.pricing-circle.blue:hover .description {
		color: var(--primary-blue);
	}

	/* Círculo blanco con borde azul */
	.pricing-circle.white {
		border: 4px solid transparent;
		border-radius: 50%;
		background-image: linear-gradient(white, white), linear-gradient(250deg, var(--primary-blue), var(--primary-blue), transparent);
		background-origin: border-box;
		background-clip: padding-box, border-box;
		background-color: white;
	}

	.pricing-circle.white .price {
		color: var(--primary-blue);
	}

	.pricing-circle.white .description {
		color: var(--primary-blue);
	}

	/* Círculo azul */
	.pricing-circle.blue {
		background-color: var(--primary-blue);
		color: white;
	}

	.price-content {
		display: flex;
		flex-direction: column;
		align-items: center;
		justify-content: center;
		width: 100%;
		height: 100%;
	}

	
	.price {
		font-size: 1.8rem;
		font-weight: 700;
		margin-bottom: 1rem;
		font-family: var(--font-heading);
		transition: color 0.3s ease;
	}
	
	.pricing-circle.blue .price {
		color: white;
	}

	.description {
		font-size: 0.9rem;
		line-height: 1.3;
		transition: color 0.3s ease;
	}
	
	.pricing-circle.blue .description {
		color: white;
	}
	
	/* Pricing popup styles */
	.pricing-popup {
		position: absolute;
		bottom: calc(100% + 15px);
		left: 50%;
		transform: translateX(-50%) translateY(10px);
		background-color: var(--primary-blue);
		border-radius: 8px;
		box-shadow: 0 4px 15px rgba(0, 0, 0, 0.1);
		color: var(--white);
		font-family: 'Syncopate', sans-serif;
		padding: 15px;
		width: 250px;
		visibility: hidden;
		opacity: 0;
		transition: all 0.3s ease-in-out;
		z-index: 20;
		pointer-events: none;
	}
	
	.pricing-circle.blue:hover .pricing-popup {
		visibility: visible;
		opacity: 1;
		transform: translateX(-50%) translateY(0);
	}
	
	/* Arrow for popup */
	.pricing-popup::after {
		content: '';
		position: absolute;
		top: 100%;
		left: 50%;
		transform: translateX(-50%);
		border-width: 8px;
		border-style: solid;
		border-color: var(--primary-blue) transparent transparent transparent;
	}
	
	.pricing-popup p {
		margin: 0;
		font-size: 0.85rem;
		line-height: 1.4;
		text-align: center;
	}

	
	@media (max-width: 767px) {
		.pricing-section {
			padding: 3rem 0;
		}
		
		.section-header {
			margin-bottom: 2.5rem;
			padding: 0 1rem;
		}
		
		.section-header h2 {
			font-size: 1.8rem;
		}
		
		.section-header p {
			font-size: 1rem;
		}
		
		.pricing-circles {
			gap: 3rem 2rem;
		}
		
		.pricing-circle {
			width: 160px;
			height: 160px;
		}

		.price {
			font-size: 1.3rem;
			margin-bottom: 0.5rem;
		}

		.description {
			font-size: 0.75rem;
		}
		
		.pricing-popup {
			width: 200px;
			padding: 12px;
		}
		
		.pricing-popup p {
			font-size: 0.75rem;
		}
	}
	
	@media (max-width: 400px) {
		.pricing-circles {
			gap: 2rem 1.5rem;
		}
		
		.pricing-circle {
			width: 140px;
			height: 140px;
			padding: 1rem;
		}
		
		.price {
			font-size: 1.1rem;
			margin-bottom: 0.4rem;
		}
		
		.description {
			font-size: 0.7rem;
			line-height: 1.2;
		}
	}
</style>
