---
const baseUrl = import.meta.env.BASE_URL;
---

<script>
	import Swiper from 'swiper';
	import { Navigation, Pagination } from 'swiper/modules';
	
	// Define the updateCardStyles function outside to avoid scope issues
	function updateCardStyles(swiperInstance) {
		const slides = document.querySelectorAll('.swiper-slide');
		const activeIndex = swiperInstance.activeIndex;
		
		// Reset all cards to secondary style (blue)
		slides.forEach((slide) => {
			const card = slide.querySelector('.card');
			if (card) {
				card.classList.remove('card-inverted', 'card-simple');
				card.classList.add('card-simple');
				card.classList.remove('card-animated');
			}
		});
		
		// Apply primary style (white) to the first visible card
		if (slides[activeIndex]) {
			const firstCard = slides[activeIndex].querySelector('.card');
			if (firstCard) {
				firstCard.classList.remove('card-simple');
				firstCard.classList.add('card-inverted', 'card-animated');
			}
		}
	}
	
	document.addEventListener('DOMContentLoaded', () => {
		const swiper = new Swiper('.swiper-container', {
			modules: [Navigation, Pagination],
			slidesPerView: 1,
			spaceBetween: 20,
			pagination: {
				el: '.swiper-pagination',
				clickable: true,
			},
			navigation: {
				nextEl: '.swiper-button-next',
				prevEl: '.swiper-button-prev',
			},
			breakpoints: {
				// when window width is >= 768px
				768: {
					slidesPerView: 2,
					spaceBetween: 30
				}
			},
			on: {
				init: function() {
					updateCardStyles(this);
				},
				slideChange: function() {
					updateCardStyles(this);
				},
				breakpoint: function() {
					updateCardStyles(this);
				}
			}
		});
	});
</script>

<section class="how-it-works" id="como-funciona">
	<div class="container">
		<div class="section-header">
			<h2>¿Cómo funciona?</h2>
		</div>
		<div class="swiper-container">
			<div class="swiper-wrapper">
				<!-- Slide 1-2 -->
				<div class="swiper-slide">
					<div class="card">
						<div class="card-number">1</div>
						<div class="card-content">
							<h3>Descargá la app "ESCOBAR 360"</h3>
							<p>En Google Play o App Store.</p>
						</div>
					</div>
				</div>
				<div class="swiper-slide">
					<div class="card">
						<div class="card-number">2</div>
						<div class="card-content">
							<h3>Escaneá el QR de la bici</h3>
							<p>Encendé el bluetooth de tu
								celular y escaneá el QR de
								la bicicleta. También podés
								ingresar la matrícula de la
								bicicleta ubicada a los
								costados de la misma.</p>
						</div>
						<div class="card-image">
							<img src={`${import.meta.env.BASE_URL}/images/step2.png`} alt="Escanear QR">
						</div>
					</div>
				</div>

				<!-- Slide 3-4 -->
				<div class="swiper-slide">
					<div class="card">
						<div class="card-number">3</div>
						<div class="card-content">
							<h3>Quitá la traba U</h3>
							<p>Esperá el "PIP" y quitá la traba U. ¡Ya casi estás listo para arrancar tu viaje!</p>
						</div>
						<div class="card-image">
							<img src={`${import.meta.env.BASE_URL}/images/step3.png`} alt="Quitar traba U">
						</div>
					</div>
				</div>
				<div class="swiper-slide">
					<div class="card">
						<div class="card-number">4</div>
						<div class="card-content">
							<h3>¡No viajes con la traba U en la mano!</h3>
							<p>Una vez que la hayas sacado, encastrala en los círculos al costado, para viajar tranquilo y seguro.</p>
						</div>
						<div class="card-image">
							<img src={`${import.meta.env.BASE_URL}/images/step4.png`} alt="Encastrar traba U">
						</div>
					</div>
				</div>

				<!-- Slide 5 -->
				<div class="swiper-slide">
					<div class="card">
						<div class="card-number">5</div>
						<div class="card-content">
							<h3>Final del viaje</h3>
							<p>Una vez finalizado el trayecto, dirigite a la estación más cercana, apoyá la bicicleta en uno de los bicicleteros y bajá el pie de apoyo. Colocá la traba U vinculando la bicicleta al bicicletero.</p>
						</div>
					</div>
				</div>
			</div>
			
			<!-- Agregar navegación y paginación -->
			<div class="swiper-pagination"></div>
			<div class="swiper-button-next"></div>
			<div class="swiper-button-prev"></div>
		</div>
	</div>
</section>

<style>
	.section-header {
		text-align: center;
		margin-bottom: 4rem;
	}

	.section-header h2 {
		font-size: 2.5rem;
		margin-bottom: 1rem;
		font-family: 'Syncopate', sans-serif;
		color: var(--primary-blue);
		text-transform: uppercase;
		letter-spacing: 0.05em;
	}

	.swiper-container {
		max-width: 1200px;
		margin: 0 auto;
		padding: 2rem 1rem;
		position: relative;
		overflow: hidden;
	}
	
	.swiper-slide {
		display: flex;
		justify-content: center;
		align-items: center;
		padding-top: 2rem;
	}

	.card {
		color: var(--white);
		background-color: var(--primary-blue);
		border-radius: 15%;
		padding: 1.5rem;
		position: relative;
		width: 100%;
		max-width: 300px;
		aspect-ratio: 1/1;
		display: flex;
		flex-direction: column;
		box-shadow: 0 4px 20px rgba(0, 0, 0, 0.15);
		overflow: visible;
		margin: 0 auto;
		box-sizing: border-box;
		transition: all 0.4s ease-in-out;
	}
	
	.card-inverted {
		background-color: white;
		color: var(--primary-blue);
		border: 2px solid var(--primary-blue);
		transform: scale(1.05);
	}
	
	.card-animated {
		animation: xs-pulse 1s ease-in-out;
	}

	.card-simple {
		justify-content: center;
	}

	.card-number {
		position: absolute;
		top: -45px;
		left: calc(50% - 35px);
		width: 70px;
		height: 70px;
		border-radius: 50%;
		background-color: white;
		display: flex;
		align-items: center;
		justify-content: center;
		font-size: 2.5rem;
		font-weight: 700;
		color: var(--primary-blue);
		font-family: 'Syncopate', sans-serif;
		border: 2px solid var(--primary-blue);
		box-shadow: 0 2px 5px rgba(0, 0, 0, 0.1);
	}
	
	.card-inverted .card-number {
		background-color: var(--primary-blue);
		color: white;
		border: 2px solid white;
	}

	.card-content {
		margin-top: 0.5rem;
		z-index: 1;
		overflow: hidden;
		display: flex;
		flex-direction: column;
		height: 100%;
	}

	.card-content h3 {
		color: white;
		font-family: 'Syncopate', sans-serif;
		font-weight: 700;
		margin: 0.75rem 0;
		font-size: 1.2rem;
		text-transform: uppercase;
		line-height: 1.2;
	}
	
	.card-inverted .card-content h3 {
		color: var(--primary-blue);
	}

	.card-content p {
		color: white;
		font-family: 'Poppins', sans-serif;
		font-size: 1rem;
		line-height: 1.3;
		margin-top: 0;
	}
	
	.card-inverted .card-content p {
		color: var(--primary-blue);
	}

	.card-image {
		position: absolute;
		bottom: -20px;
		right: -20px;
		width: 90px;
		height: 90px;
		border-radius: 50%;
		overflow: hidden;
		border: 3px solid white;
		z-index: 2;
		box-shadow: 0 4px 8px rgba(0, 0, 0, 0.2);
		background-color: white;
	}

	.card-image img {
		width: 100%;
		height: 100%;
		object-fit: cover;
		border-radius: 50%;
	}

	@media (max-width: 768px) {
		.card {
			width: 220px;
			height: 220px;
		}
		
		.card-image {
			width: 70px;
			height: 70px;
			right: -15px;
			bottom: -15px;
		}
		
		.card-content h3 {
			font-size: 0.9rem;
			margin-bottom: 0.5rem;
		}
		
		.card-content p {
			font-size: 0.7rem;
			line-height: 1.2;
		}
		
		.swiper-button-next, 
		.swiper-button-prev {
			display: none;
		}
	}
	
	@media (max-width: 480px) {
		.cards-container {
			gap: 2rem;
		}
		
		.card {
			width: 150px;
			height: 150px;
			padding: 1rem;
		}
	}
	/* Estilos de navegación del carrusel */
	.swiper-button-next, 
	.swiper-button-prev {
		color: var(--primary-blue);
		font-weight: bold;
	}

	.swiper-pagination-bullet {
		width: 12px;
		height: 12px;
		opacity: 1;
		background: rgba(44, 78, 155, 0.2);
	}

	.swiper-pagination-bullet-active {
		background: var(--primary-blue);
	}
	
	.swiper-button-next:after, 
	.swiper-button-prev:after {
		font-size: 1.5rem;
	}
	
	@keyframes xs-pulse {
		0% { transform: scale(1); }
		50% { transform: scale(1.08); }
		100% { transform: scale(1.05); }
	}
</style>

<!-- Importación de estilos CSS de Swiper -->
<link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/swiper@11/swiper-bundle.min.css" />
<!-- Importación de librería de Swiper -->
<script is:inline src="https://cdn.jsdelivr.net/npm/swiper@11/swiper-bundle.min.js"></script>
