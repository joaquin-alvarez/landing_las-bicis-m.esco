---
---

<section class="stations-section" id="estaciones">
	<div class="container">
		<div class="section-header">
			<h2>¿Dónde las encuentro?</h2>
			<p>Contamos con 34 estaciones en Belén de Escobar, Ingeniero Maschwitz y Garin.</p>
		</div>
		
		<div class="stations-content">
			<div class="map-container">
				<iframe src="https://www.google.com/maps/d/u/2/embed?mid=1D9laS1EMCV728UfVVv5fj0LgdpQgdZ8&ehbc=2E312F&noprof=1" width="100%" height="600px" style="border: none;"></iframe>
			</div>
			
			<!-- <div class="stations-stats">
				<div class="stat-item">
					<div class="stat-number">150+</div>
					<div class="stat-label">Estaciones</div>
				</div>
				<div class="stat-item">
					<div class="stat-number">2,500+</div>
					<div class="stat-label">Bicicletas</div>
				</div>
				<div class="stat-item">
					<div class="stat-number">24/7</div>
					<div class="stat-label">Disponible</div>
				</div>
				<div class="stat-item">
					<div class="stat-number">95%</div>
					<div class="stat-label">Disponibilidad</div>
				</div>
			</div> -->
		</div>
	</div>
</section>

<style>
	.stations-section {
		background-color: var(--gray-100);
	}

	.section-header {
		text-align: center;
		margin-bottom: 4rem;
	}

	.section-header h2 {
		color: var(--primary-blue);
		margin-bottom: 1rem;
	}

	.section-header p {
		font-size: 1.1rem;
		color: var(--primary-blue);
		max-width: 600px;
		margin: 0 auto;
	}

	.stations-content {
		display: flex;
		justify-content: center;
	}

	.map-container {
		background: var(--white);
		border-radius: var(--border-radius-lg);
		padding: 2rem;
		box-shadow: var(--shadow-md);
		width: 80%;
		max-width: 1000px;
	}

	.map-placeholder {
		height: 400px;
		background: linear-gradient(135deg, var(--light-blue) 0%, var(--gray-200) 100%);
		border-radius: var(--border-radius);
		display: flex;
		align-items: center;
		justify-content: center;
		font-weight: 500;
		color: var(--primary-blue);
		font-size: 1.1rem;
	}

	.stations-stats {
		display: grid;
		grid-template-columns: repeat(2, 1fr);
		gap: 2rem;
	}

	.stat-item {
		text-align: center;
		padding: 2rem;
		background: var(--white);
		border-radius: var(--border-radius-lg);
		box-shadow: var(--shadow-sm);
		transition: transform 0.3s ease;
	}

	.stat-item:hover {
		transform: translateY(-4px);
	}

	.stat-number {
		font-size: 2.5rem;
		font-weight: 700;
		color: var(--primary-blue);
		font-family: var(--font-heading);
		line-height: 1;
		margin-bottom: 0.5rem;
	}

	.stat-label {
		font-size: 1rem;
		color: var(--gray-600);
		font-weight: 500;
	}

	@media (max-width: 767px) {
		.stations-section {
			padding: 3rem 0;
		}

		.section-header {
			margin-bottom: 2.5rem;
			padding: 0 1rem;
		}

		.section-header h2 {
			font-size: 1.8rem;
		}

		.section-header p {
			font-size: 1rem;
		}

		.map-container {
			width: 90%;
			padding: 1rem;
		}

		.map-container iframe {
			height: 450px;
		}
	}

	@media (max-width: 480px) {
		.stations-section {
			padding: 2rem 0;
		}

		.section-header {
			margin-bottom: 2rem;
		}

		.section-header h2 {
			font-size: 1.6rem;
		}

		.section-header p {
			font-size: 0.9rem;
			line-height: 1.4;
		}

		.map-container {
			width: 95%;
			padding: 0.75rem;
		}

		.map-container iframe {
			height: 350px;
		}
	}

	@media (min-width: 768px) {
		.map-placeholder {
			height: 500px;
		}
	}
</style>
