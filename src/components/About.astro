---
---

<section class="about-section" id="las-bicis">
	<div class="container">
		<div class="about-layout">
			<div class="about-content">
				<h2>Las Bicis</h2>
				<p class="about-description">
					Somos más que un sistema de bicicletas compartidas. Somos un movimiento 
					hacia una ciudad más sostenible, saludable y conectada.
				</p>
				<div class="about-features">
					<div class="feature-item">
						<h4>🌱 Sostenible</h4>
						<p>Reducimos la huella de carbono con cada viaje compartido</p>
					</div>
					<div class="feature-item">
						<h4>💪 Saludable</h4>
						<p>Promovemos un estilo de vida activo y saludable</p>
					</div>
					<div class="feature-item">
						<h4>🤝 Conectada</h4>
						<p>Unimos comunidades a través de la movilidad compartida</p>
					</div>
				</div>
				<a href="#" class="btn btn-primary">Conoce más sobre nosotros</a>
			</div>
			<div class="about-image">
				<div class="image-placeholder">
					[IMAGEN BICICLETA]
				</div>
			</div>
		</div>
	</div>
</section>

<style>
	.about-section {
		background-color: var(--gray-100);
	}

	.about-layout {
		display: grid;
		grid-template-columns: 1fr;
		gap: 3rem;
		align-items: center;
	}

	.about-content h2 {
		color: var(--primary-blue);
		margin-bottom: 1.5rem;
		font-size: 2.5rem;
	}

	.about-description {
		font-size: 1.1rem;
		color: var(--gray-600);
		line-height: 1.6;
		margin-bottom: 3rem;
	}

	.about-features {
		margin-bottom: 3rem;
	}

	.feature-item {
		margin-bottom: 2rem;
	}

	.feature-item h4 {
		color: var(--primary-blue);
		margin-bottom: 0.5rem;
		font-size: 1.1rem;
	}

	.feature-item p {
		color: var(--gray-600);
		line-height: 1.5;
	}

	.about-image {
		text-align: center;
	}

	.image-placeholder {
		width: 100%;
		max-width: 400px;
		height: 300px;
		background: linear-gradient(135deg, var(--light-blue) 0%, var(--accent-green) 100%);
		border-radius: var(--border-radius-lg);
		display: flex;
		align-items: center;
		justify-content: center;
		font-weight: 500;
		color: var(--primary-blue);
		font-size: 1.1rem;
		margin: 0 auto;
		box-shadow: var(--shadow-md);
	}

	@media (min-width: 768px) {
		.about-layout {
			grid-template-columns: 1.5fr 1fr;
		}

		.about-content {
			text-align: left;
		}

		.image-placeholder {
			height: 400px;
		}
	}

	@media (min-width: 1024px) {
		.about-content h2 {
			font-size: 3rem;
		}
		
		.image-placeholder {
			height: 500px;
		}
	}
</style>
